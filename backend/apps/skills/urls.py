"""URL configuration for skills app"""
from django.urls import path

app_name = 'skills'

urlpatterns = [
    # path('', SkillListView.as_view(), name='skill-list'),
    # path('autocomplete/', SkillAutocompleteView.as_view(), name='autocomplete'),
]
