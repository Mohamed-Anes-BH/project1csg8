"""URL configuration for moderation app"""
from django.urls import path

app_name = 'moderation'

urlpatterns = [
    # path('pending-companies/', PendingCompaniesView.as_view(), name='pending'),
    # path('approve/<int:pk>/', ApproveCompanyView.as_view(), name='approve'),
]
